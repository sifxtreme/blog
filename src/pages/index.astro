---
// The homepage for whichever design is active (DESIGN in consts.ts).
import { DESIGN } from '../consts';
import ClassicHome from '../designs/classic/Home.astro';
import ReceiptHome from '../designs/receipt/Home.astro';

const Home = DESIGN === 'receipt' ? ReceiptHome : ClassicHome;
---

<Home />
